---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hajimari-config-v1
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: rook-ceph
  resources:
    requests:
      storage: 128Mi
